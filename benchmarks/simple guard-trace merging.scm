(begin (define (loop i)
         (display "before if: i = ") (display i)
         (newline)
         (if (< i 5)
             (begin (display "i < 5")
                    (newline))
             (begin (display "i >= 5")
                    (newline)))
         (display "after if")
         (newline)
         (if (> i 0)
             (loop (- i 1))))
       (loop 15))