(begin (define (loop i)
         (display "before if")
         (if (< i 5)
             (display "i < 5")
             (display "i >= 5"))
         (display "after if")
         (if (> i 0)
             (loop (- i 1))))
       (loop 15))